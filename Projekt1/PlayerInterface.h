#pragma once
class PlayerInterface
{
public:
	PlayerInterface();
	virtual ~PlayerInterface();
	//void moveHero(const HeroInstance *h, Path path);
	/*
	void stopMovement();
	
	void initMovement(const TryMoveHero &details, const CGHeroInstance * ho, const int3 &hp);//initializing objects and performing first step of move
	void heroVisit(const CGHeroInstance * visitor, const CGObjectInstance * visitedObj, bool start);
	void heroVisitsTown(const CGHeroInstance* hero, const CGTownInstance * town);
	*/
};

